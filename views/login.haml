%h1 Login to Related Products Application
%p First you have to register this app with a shop to allow access to its private admin data.
%form{:action=>'/login/authenticate', :method=>'post'}
  %label{:for=>'shop'}
    %strong The URL of the Shop
    %span.hint (or just the subdomain if it's at myshopify.com)
  %p
    %input{:type=>'text',:name=>'shop'}
    %input{:type=>'submit', :value=>'Authenticate'}
